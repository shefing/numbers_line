# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches: [ "main" ]
    paths:
      - library/math/numbersLine/**
      - .github/workflows/numbersLine-firebase-hosting-merge.yml
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: yarn && yarn build
        working-directory: library/math/numbersLine
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entrypoint: library/math/numbersLine
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NUMBERS_LINE }}'
          channelId: live
          projectId: numbers-line
